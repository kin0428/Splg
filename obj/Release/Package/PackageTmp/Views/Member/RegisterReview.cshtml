@model Splg.Models.ViewModel.MemberRegistViewModel
@{
    ViewBag.Title = "会員登録 - 確認";
}
<div class="bottom">
    <div class="mem_block01 clear">
        <div class="mem_b1_1">
            <h2>スポログにユーザー登録(無料)する</h2>
            <ul>
                <li>STEP 1<br />入力</a></li>
                <li><span>STEP 2<br />確認</span></li>
                <li>STEP 3<br />メール送信</a></li>
                <li>STEP 4<br />登録完了</a></li>
            </ul>
        </div>
        <div class="mem_b2_1">
            <p class="mail_title">
                <span class="bold">入力内容を確認し、仮登録を行ってください。 </span><br />
                本登録に必要なURLを、入力したメールアドレス宛に送信します。
            </p>
            <table cellpadding="0" cellspacing="0" summary="confirm">
                <tr>
                    <th>メールアドレス</th>
                    <td>@Html.Raw(Model.Email)</td>
                </tr>
                <tr>
                    <th>パスワード</th>
                    <td>
                        <p>@(Utils.FormatStringToAsterisk(Model.Password))</p>
                        <p>セキュリティ上、伏せて表示しています</p>
                </td>
            <tr>
                <th>お知らせ</th>
                <td>@( Model.ReceiveEmail ? "受け取る" : "受け取らない")</td>
            </tr>
            </table>
        @using (Html.BeginForm("RegisterReview", "Member", FormMethod.Post, new { @id = "sendMailForActivation", role = "form" }))
        {
            @Html.AntiForgeryToken()
            <p class="board_link"><a href="javascript:void(0);" onclick="document.getElementById('sendMailForActivation').submit();">本登録用URLをメールで受け取る</a></p>
        }
        </div>
    </div>
</div>
